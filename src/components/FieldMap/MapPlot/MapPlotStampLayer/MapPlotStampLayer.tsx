'use client';

import React from 'react';

interface MapPlotStampLayerProps {
  children?: React.ReactNode;
}

export function MapPlotStampLayer({ children }: MapPlotStampLayerProps) {
  return <>{children}</>;
}
