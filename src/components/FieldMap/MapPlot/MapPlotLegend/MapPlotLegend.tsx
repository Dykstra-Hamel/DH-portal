'use client';

import React from 'react';

interface MapPlotLegendProps {
  children?: React.ReactNode;
}

export function MapPlotLegend({ children }: MapPlotLegendProps) {
  return <>{children}</>;
}
