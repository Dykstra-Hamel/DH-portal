'use client';

import React from 'react';

interface MapPlotToolbarProps {
  children?: React.ReactNode;
}

export function MapPlotToolbar({ children }: MapPlotToolbarProps) {
  return <>{children}</>;
}
